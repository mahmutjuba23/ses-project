extends ../layout

block content
  style.
    .users-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 15px;
      animation: fadeIn 0.4s ease-out;
    }
    body::-webkit-scrollbar, dialog::-webkit-scrollbar, textarea::-webkit-scrollbar { display: none; }
    body, dialog, textarea { -ms-overflow-style: none; scrollbar-width: none; }
    body:has(dialog[open]) { overflow: hidden; }

    .admin-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 1px solid var(--border-color);
    }
    .admin-header h1 { margin: 0; color: var(--text-primary); font-size: 1.5rem; font-weight: 600; }
    .admin-header p  { margin: 5px 0 0; color: var(--text-secondary); font-size: 0.85rem; }
    .header-actions { display: flex; gap: 10px; align-items: center; }

    .card {
      background: var(--bg-card);
      border-radius: 12px;
      box-shadow: var(--shadow);
      padding: 15px;
      margin-bottom: 20px;
      border: 1px solid var(--border-color);
    }
    .table-responsive { overflow-x: auto; border-radius: 8px; }

    table.modern-table { width: 100%; border-collapse: collapse; text-align: left; }
    table.modern-table th {
      background-color: var(--bg-secondary); color: var(--text-secondary); font-weight: 600;
      padding: 10px; border-bottom: 2px solid var(--border-color);
      font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.5px;
    }
    table.modern-table td {
      padding: 10px; border-bottom: 1px solid rgba(0,0,0,0.05);
      color: var(--text-primary); font-size: 0.85rem; vertical-align: middle;
    }
    table.modern-table tbody tr:hover { background-color: rgba(0,0,0,0.02); }

    .role-pill {
      display: inline-flex; align-items: center; gap: 6px;
      background: rgba(244,63,94,0.1); color: var(--accent);
      padding: 4px 10px; border-radius: 20px; font-size: 0.8rem; font-weight: 500;
      border: 1px solid rgba(244,63,94,0.25); margin: 2px;
    }
    .role-pill button {
      background: none; border: none; cursor: pointer; color: var(--accent);
      font-size: 1rem; line-height: 1; padding: 0; transition: color 0.2s;
    }
    .role-pill button:hover { color: var(--danger); }

    .btn-modern {
      padding: 8px 15px; border-radius: 8px; font-weight: 500; font-size: 0.85rem;
      transition: all 0.2s; border: none; cursor: pointer;
      display: inline-flex; align-items: center; justify-content: center;
      text-decoration: none; gap: 6px;
    }
    .btn-modern-primary {
      background: var(--gradient-1); color: white;
      box-shadow: 0 4px 15px var(--accent-glow);
    }
    .btn-modern-primary:hover { transform: translateY(-1px); box-shadow: 0 6px 20px var(--accent-glow); }
    .btn-modern-outline {
      background: transparent; color: var(--text-secondary);
      border: 1px solid var(--border-color);
    }
    .btn-modern-outline:hover { color: var(--text-primary); border-color: var(--accent); }
    .btn-modern-sm { padding: 4px 8px; font-size: 0.75rem; border-radius: 6px; }

    dialog::backdrop {
      background-color: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(8px);
    }
    dialog {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      margin: 0;
    }
    dialog[open] {
      animation: modalFadeIn 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }
    @keyframes modalFadeIn {
      from { opacity: 0; transform: translate(-50%, -45%) scale(0.95); }
      to { opacity: 1; transform: translate(-50%, -50%) scale(1); }
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Dropdown Styles */
    .dropdown {
      position: relative;
      display: inline-block;
    }
    .dropdown-content {
      display: none;
      position: absolute;
      right: 0;
      background-color: var(--bg-card);
      min-width: 180px;
      box-shadow: var(--shadow-lg);
      z-index: 10;
      border-radius: 8px;
      border: 1px solid var(--border-color);
      overflow: hidden;
      padding: 5px 0;
    }
    .dropdown:hover .dropdown-content {
      display: block;
      animation: fadeIn 0.2s ease-out;
    }
    .dropdown-item {
      padding: 8px 15px;
      display: block;
      width: 100%;
      text-align: left;
      background: none;
      border: none;
      color: var(--text-primary);
      cursor: pointer;
      font-size: 0.85rem;
      transition: background 0.2s;
    }
    .dropdown-item:hover {
      background-color: rgba(0, 0, 0, 0.05);
      color: var(--accent);
    }

    .form-control-modern {
      width: 100%; padding: 8px 12px;
      background: var(--bg-input); border: 1px solid var(--border-color);
      color: var(--text-primary); border-radius: 6px; font-size: 0.85rem;
      transition: border-color 0.2s, box-shadow 0.2s; box-sizing: border-box;
    }
    .form-control-modern:focus {
      outline: none; border-color: var(--border-focus);
      box-shadow: 0 0 0 3px var(--accent-glow);
    }
    .form-group-modern { margin-bottom: 12px; }
    .form-group-modern label {
      display: block; margin-bottom: 4px; font-weight: 500;
      color: var(--text-secondary); font-size: 0.8rem;
    }

  .users-container
    .admin-header
      div
        h1 Manage Users & Roles
        p Search users and assign roles below.

    .card
      .table-responsive
        table.modern-table
          thead
            tr
              th ID
              th Full Name
              th Email
              th Current Roles
              th Manage
          tbody
            each u in users
              tr
                td(style="color: var(--text-muted); font-size: 0.85rem;")= u.id
                td(style="font-weight: 500;")= u.full_name
                td(style="color: var(--text-secondary);")= u.email
                td
                  if u.Roles && u.Roles.length
                    each r in u.Roles
                      span.role-pill
                        | #{r.name}
                        form(action="/admin/users/remove-role" method="POST" style="display:inline; margin:0;")
                          input(type="hidden" name="user_id" value=u.id)
                          input(type="hidden" name="role_id" value=r.id)
                          button(type="submit" title="Remove role") ×
                  else
                    span(style="color: var(--text-muted); font-style: italic; font-size: 0.9rem;") No roles
                td
                  .dropdown
                    button.btn-modern.btn-modern-primary.btn-modern-sm
                      | Manage Roles ▾
                    .dropdown-content
                      each role in allRoles
                        form(action="/admin/users/assign-role" method="POST" style="margin: 0;")
                          input(type="hidden" name="user_id" value=u.id)
                          input(type="hidden" name="role_id" value=role.id)
                          button.dropdown-item(type="submit") + Assign #{role.name}
